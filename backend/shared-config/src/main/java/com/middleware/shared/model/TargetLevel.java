package com.middleware.shared.model;

/**
 * Enum representing the target level of a mapping rule.
 * Used to clearly distinguish between header and line level mappings.
 */
public enum TargetLevel {
    HEADER,
    LINE
} 